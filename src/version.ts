// Single source of truth for the version. Imported by cli.ts (usage,
// status), plugin.ts (plugin manifest + cache path), and payload.ts
// (telemetry resource attributes). Bump here and nowhere else.
export const VERSION = "0.1.0";
